# -*- coding: utf-8 -*-
$:.unshift('/Library/RubyMotion/lib')
require 'motion/project/template/ios'
require './lib/motion-assets-library'

Motion::Project::App.setup do |app|
  app.name = 'motion-assets-library'

  app.frameworks += %w(AssetsLibrary)
end
